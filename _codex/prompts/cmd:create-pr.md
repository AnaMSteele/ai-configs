Commit all changes and create a new pull request with all commits squashed and a summary of all changes as the PR description.

If the work is tied to a Linear issue, the PR title must start with the Linear issue key and include the Linear issue title, for example `NOD-632: Ccore health auth guidance`. Do not create a Linear-backed PR using only the latest commit subject unless it already satisfies that format.
